streamlit==1.36.0
pandas==2.2.2       # If you use pandas elsewhere in your app, include it
pymongo==4.8.0      # If you use pymongo elsewhere, include it
Pillow==10.4.0
geopy==2.4.1
plotly==5.21.0      # If you use plotly elsewhere, include it
pytz==2024.1        # If you use pytz elsewhere, include it
requests==2.32.3    # Or the latest stable version
toml==0.10.2        # Or the latest stable version
streamlit_js_eval==0.0.1  # Check the latest version on PyPI
